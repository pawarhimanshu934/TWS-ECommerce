kind: Service
apiVersion: v1
metadata:
  name: easyshop-service
  namespace: easyshop-ns
  
spec:
  type: NodePort
  selector:
    app: easyshop
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 31500
